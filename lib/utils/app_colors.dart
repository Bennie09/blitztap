import 'package:flutter/material.dart';

class AppColors {
  static const background = Color(0xFF262E38);   // Main app background (Jet Black)
  static const surface = Color(0xFF12161C);      // Cards, panels, timer surfaces
  static const active = Color(0xFF59D383);       // Active player / highlight (new green)
  static const inactive = Color(0xFF2A2F38);     // Inactive player
  static const danger = Color(0xFFE5533D);       // Low-time warning
  static const ripple = Color(0xFF3B4655);       // Tap ripple / subtle effects

  static const textPrimary = Color(0xFFFFFFFF);  // Main text
  static const textSecondary = Color(0xFF9AA4AF);// Secondary text
}
